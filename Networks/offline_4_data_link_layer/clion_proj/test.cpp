#include <iostream>
#include <stdio.h>
using namespace std;

char * to_binary(int n){
    char bin[33];
    while(n){
        if(n % 2)
        n /= 2;
    }
}

int main(){
    char s[20];
    char t[20] = "temp";
    string str(t);
    cout << str << endl;
    // strcpy(s, t);
    // printf("%s : %d", s, strlen(t));
}